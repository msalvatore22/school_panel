<%= bootstrap_form_with(model: @student, local: true) do |f| %>
<%= f.text_field :first_name, label: "First Name:" %>
<%= f.text_field :last_name, label: "Last Name:" %>
<%= f.select :age, (12..60), label: "Age:" %>
<%= f.select :highest_completed_education, ["high-school", "college", "masters", "PhD"], label: "Highest Completed Education:" %>
<%= f.submit "Create Student" %>
<% end %>